describe("", () => {
    test("", () => {
        const array = ["hello"];
        expect(array).toEqual(["hello"]);
    })

    test("", () => {
              const object = {
                cat: "Sigrid",
              };
              expect(object).toEqual({
                cat: "Sigrid",
              });
    })

    test("", () => {
        const string = "cats";
        expect(string).toBe("cats");
    })
})  